services:
  studysync:
    build: .
    ports:
      - "8080:8080"
    environment:
      DJANGO_SECRET_KEY: change-me
      MONGO_URI: mongodb://mongo:27017/studysync
    depends_on: [mongo]
  mongo:
    image: mongo:7
    volumes: [mongo_data:/data/db]
volumes:
  mongo_data:
